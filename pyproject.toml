[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "shesh-skills"
version = "0.1.0"
description = "Shesh Skills: Agent Skills library and everyday tool server"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "GPL-3.0-or-later" }
authors = [{ name = "Gagan Jain" }]
dependencies = ["fastmcp>=3.4.7,<4", "shesh-core>=0.1"]

[project.optional-dependencies]
dev = ["pytest>=9.1,<10", "ruff>=0.16,<1"]

[project.scripts]
shesh-skills-mcp = "shesh_skills.server:main"

[tool.uv]
environments = ["sys_platform == 'linux'"]

[tool.uv.sources]
# shesh-audit was never a PyPI distribution: it is a package inside shesh-core
# (ADR-0019). Depending on it by name could never resolve.
shesh-core = { path = "../shesh-core", editable = true }

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# BLE+TRY are errors, not style: blind `except Exception` is where silent
# failures live. See shesh-ecosystem/docs (2026-08-12 silent-failure sweep).
extend-select = ["BLE", "TRY"]
select = ["E", "F", "W", "UP", "B", "SIM", "I"]
ignore = ["E402"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
